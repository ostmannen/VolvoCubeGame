using UnityEngine;
using UnityEngine.EventSystems;

public class SetUIiButton : MonoBehaviour
{
    public GameObject firstButton;

    void Start()
    {
        //EventSystem.current.SetSelectedGameObject(firstButton);
    }
}
